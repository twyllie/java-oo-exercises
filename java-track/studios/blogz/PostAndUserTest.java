package blogz;

import org.junit.Test;

public class PostAndUserTest {
	//TODO Test: hasPassword() is properly hashing passwords
	
	//TODO Test: isValidPassword() is properly checking passwords
	
	//TODO Test: isValidUsername() is properly checking for valid usernames
	// valid usernames are 4-12 characters long, start with a letter, and only contain letters, numbers, -, and _.
	
	//TODO Test: Test that the static list of users created are accessible and as expected.
	
	//TODO Test: Date is properly modifying when Post is updated.
	
	//TODO: Make sure that each item is properly being assigned a UID, and that it is (relatively) unique.
}
